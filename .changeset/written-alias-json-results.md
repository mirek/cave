---
"@cavelang/cli": minor
---

Make suggest-alias --json --write perform the requested write and return selected suggestions with the actual appended count, rather than silently returning a preview.
