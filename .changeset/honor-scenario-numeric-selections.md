---
"@cavelang/scenario": patch
---

Honor the selected query variable for numeric scenario inputs instead of replacing it with the matching row's attribute value. Retain interpolation for selected attribute values and attach attribute uncertainty only when that value slot is selected. Apply the same behavior to stored and overlay claims.
