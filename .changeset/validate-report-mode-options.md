---
"@cavelang/cli": patch
---

Reject malformed programmatic report aliases and resolve settings before store reads instead of silently disabling the requested mode. Preserve valid boolean settings and per-line template query diagnostics.
