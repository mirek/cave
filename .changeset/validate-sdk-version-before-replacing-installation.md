---
"@cavelang/cli": patch
---

Validate the exact SDK version before replacing a grammar toolchain installation, preserving the prior installation when a verified archive has missing or mismatched version metadata.
