---
"@cavelang/solver": patch
"@cavelang/cli": patch
---

Normalize Boolean and enum sensitivity values before duplicate detection so property order and extra fields cannot cause redundant sample runs.
