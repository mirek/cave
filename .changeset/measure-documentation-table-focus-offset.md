---
"@cavelang/cli": patch
---

Use the measured sticky header height when revealing focused documentation table headings, preserving horizontal position even when the header grows beyond the default anchor offset.
