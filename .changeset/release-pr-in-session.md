---
"@cavelang/core": patch
---

The pull-requests skill and `CLAUDE.md` state that the session which merges a changeset-carrying PR also verifies and merges the resulting `chore(release): version packages` PR before it ends, with the staleness, completeness, derived-manifest and CI checklist that verification runs through.
