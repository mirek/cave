---
"@cavelang/cli": patch
---

Share sensitivity-ceiling validation across viewer APIs, reports and HTTP startup. Reject null and unknown values before database work instead of silently defaulting or returning empty projections.
