---
"@cavelang/cli": patch
---

Add a Stop query control to the browser playground. It terminates a pending query's worker, retains editor text, and focuses Rebuild database so users can recover without leaving the page.
