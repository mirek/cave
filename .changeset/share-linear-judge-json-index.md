---
"@cavelang/cli": minor
---

Share the iterative JSON syntax index through the agent layer and use it in alias reply parsing, avoiding repeated decoding of overlapping malformed candidates. Export jsonValueEnds through the CLI loop surface and retain evaluation recovery behavior.
