---
"@cavelang/scenario": patch
---

Capture scenario definitions iteratively so deeply nested malformed values reach classified field validation without a JavaScript stack overflow, preserving shared references and getter traversal order.
