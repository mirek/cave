---
"@cavelang/cli": patch
---

Use the store's version-aware vocabulary refresh inside reply transactions, avoiding repeated full registry rebuilds while preserving peer-committed inverse and rename declarations.
