---
'@cavelang/cli': patch
---

Close watcher startup races, expose deterministic connect runtime hooks, identify failed watch stages, and pin URL, debounce, retry, pruning, provenance, and live automation polling end to end.
