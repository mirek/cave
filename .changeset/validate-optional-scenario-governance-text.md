---
"@cavelang/scenario": patch
---

Validate optional governance text fields consistently during recording and imported reads, rejecting non-string values without writes while preserving omitted and empty-string fields.
