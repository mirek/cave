---
"@cavelang/core": patch
"@cavelang/cli": patch
---

Keep malformed HTTP(S) source identities as plain provenance citations instead of exposing invalid navigable links. Preserve stored source identities, line spans, and existing URL escapes.
