---
"@cavelang/cli": patch
---

Launch bootstrap package-manager commands through the Windows command interpreter so pnpm, Corepack, and npm .cmd shims can run. Preserve exact-version resolution, installation output, and failure exit codes.
