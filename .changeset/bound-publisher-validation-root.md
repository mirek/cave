---
"@cavelang/core": patch
---

Bind release validation to the publisher's own checkout so inherited root overrides cannot authorize dirty source or preparation drift using another repository's clean state.
