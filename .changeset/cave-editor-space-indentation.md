---
"@cavelang/tree-sitter-cave": patch
---

Default VS Code CAVE files to two-space indentation without autodetection, avoiding
hard-tab indentation rejected by the parser while retaining explicit user overrides.
