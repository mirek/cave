---
"@cavelang/core": patch
---

Add an optional Valid at field and a changing-headcount sample to the website playground. Pass valid-time anchors through the worker to the shared query engine for coverage filtering and trajectory interpolation, with recoverable validation errors and per-sample defaults.
