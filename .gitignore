node_modules/
*.db
*.db-journal
*.db-wal
*.db-shm
coverage/
dist/
playwright-report/
test-results/
.DS_Store
*.tsbuildinfo
*.vsix

# Generated grammar artifacts — unauditable, so never committed; built on
# demand by `pnpm --filter @cavelang/tree-sitter-cave build` (tree-sitter-cli
# fetches wasi-sdk itself) and shipped only inside the npm tarball via prepack.
*.wasm
packages/tree-sitter-cave/src/

# Canonical legal files are staged into public packages by their prepack hook.
packages/*/License.md
packages/*/Authors.md
