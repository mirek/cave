/**
 * `@cavelang/cli` — the `cave` command (parse / add / query / check / export / demo).
 * Programmatic access to the same implementations lives here.
 */

export { addCommand, cave, checkCommand, commandHelp, demoCommand, exportCommand, helpCommand, highlightCommand, importCommand, parseCommand, queryCommand, usage, versionCommand } from './cli.ts'
export type { Output } from './cli.ts'
