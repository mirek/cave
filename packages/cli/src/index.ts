/**
 * `@cavelang/cli` — the `cave` command (parse / add / query / derive / act / check / export / reconstruct / demo).
 * Programmatic access to the same implementations lives here.
 */

export { actCommand, addCommand, cave, checkCommand, commandHelp, demoCommand, deriveCommand, exportCommand, helpCommand, highlightCommand, importCommand, parseCommand, queryCommand, reconstructCommand, usage, versionCommand } from './cli.ts'
export type { Output } from './cli.ts'
